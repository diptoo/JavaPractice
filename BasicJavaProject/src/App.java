import java.lang.ref.Cleaner.Cleanable;
import java.util.ArrayList;
import java.util.Scanner;

import TestClassFolder.*;

public class App {
    public static void main(String[] args) throws Exception {
        System.out.println("Hello, World!");

        new Rectangle().

        
        // ArrayList<Account> accounts = new ArrayList<>();
        // Client client = new Client(100, "dipto", "12313");
        // accounts.add(new Account(1, 1000, 1.5, client));
        // accounts.add(new Account(2, 2000, 2.5, client));

        // accounts.get(0).withdraw(1200);
        // accounts.get(0).deposit(500);
        // accounts.get(0).withdraw(1200);
        // accounts.get(1).withdraw(1200);

        // for(Account account : accounts)
        // {
        //     System.out.println("Account Id = "+account.getId() + " Withdraw " + account.countTransactions('W'));
        //     System.out.println("Account Id = "+account.getId() + " Deposit " + account.countTransactions('D'));
        // }

        // Client[] clients = new Client[2];

        // clients[0] = new Client(1, "client1", "111");
        // for(int i = 0; i < 2; i++)
        // {
        //     Account account = new Account(i, 100 + i, 1 + i);
        //     clients[0].addAccount(account);
        // }

        // clients[1] = new Client(2, "client2", "222");
        // for(int i = 0; i < 3; i++)
        // {
        //     Account account = new Account(i, 200 + i, 1 + i);
        //     clients[1].addAccount(account);
        // }

        // System.out.println(clients[0].toString());
        // System.out.println(clients[1]); // java defaults call toString

        // Account testAccount = new Account(1122, 20000, 4.5);
        // testAccount.withdraw(2500);
        // testAccount.deposit(3000);

        // System.out.println("Account Information: Id = "+ testAccount.getId()
        //                                         + " Balance = " + testAccount.getBalance());





        // Rectangle[] rectangles = new Rectangle[3];
        // //rectangles[0]. -> need to create instance first

        // Scanner input = new Scanner(System.in);
        // for(int i = 0; i < rectangles.length; i++)
        // {
        //     char choice = input.next().charAt(0);
        //     if(choice == 'y')
        //     {
        //         rectangles[i] = new Rectangle(input.nextDouble(), input.nextDouble());
        //     }
        //     else
        //     {
        //         rectangles[i] = new Rectangle();
        //     }

        //     System.out.println("Width: "+ rectangles[i].getWidth());
        // }
    }
}
